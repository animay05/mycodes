#include <stdio.h>

int main(void) {

    int amt;
    float balance;
    scanf("%d %f", %amt, %balance);

    if(amt %5 !=0) {
   printf(%.2f,balance);
   return 0;
}

    if(amt+0.5>balance) {
    printf(%.2f,balance);
    return 0;
}
     balance=balance-amt-0.5;
    return 0;
}
